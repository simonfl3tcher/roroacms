require 'rails_helper'

RSpec.describe Setting, :type => :model do

	it "should get the value for the given setting"
	it "get all settings"
	it "is invalid if articles slug is blank"
	it "is invalid if category slug is blank"
	it "is invalid if tag slug is blank"
	it "is invalid if smtp username slug is blank"
	it "is invalid if smtp password slug is blank"
	it "is invalid if smtp authentication slug is blank"
	it "should save the value against the given field"
	it "should return the mail settings"	

end