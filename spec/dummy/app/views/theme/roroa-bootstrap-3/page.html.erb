
	<% if !is_homepage?  %>

   		<div class="row">
    		<div class="col-lg-12 col-md-12">
    			<h1 class="page-header"><%= obtain_the_title %></h1>
    		</div>
    	</div>


    	<div class="row">
    		<div class="col-lg-12 col-md-12">
    			<%= render_breadcrumbs %>
    		</div>
    	</div>
	<% end %>
    <div class="row">
    	<div class="col-lg-12 col-md-12">
            <% if has_cover_image? %>
                <img src="<%= obtain_cover_image %>" width="1140">
            <% end %>
            <br />
            <br />
    		<%= obtain_the_content %>
    	</div>
    </div>
    <!-- /.row -->
